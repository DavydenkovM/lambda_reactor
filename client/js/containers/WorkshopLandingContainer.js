import React, { PropTypes  } from 'react';
import { connect  } from 'react-redux';
import { bindActionCreators  } from 'redux';

// import * as WorkshopLandingActions from 'actions/WorkshopLandingActions';

// import Header from 'components/shared/Header/Header';
// import Footer from 'components/shared/Footer/Footer';

// import css from './WorkshopLandingContainer.sass';

function select(state) {
  return {
  };
}

class WorkshopLandingContainer extends React.Component {

}

export default connect(select)(WorkshopLandingContainer);

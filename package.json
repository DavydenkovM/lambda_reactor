{
  "name": "lambda_reactor-react-redux",
  "version": "0.0.1",
  "description": "project with react and redux.",
  "engines": {
    "node": "6.2.0",
    "npm": "3.8.9"
  },
  "repository": {
    "type": "git",
    "url": "ssh://git@github.com/DavydenkovM/lambda_reactor.git"
  },
  "author": "Davydenkov Mihail",
  "scripts": {
    "postinstall": "cd client && npm install",
    "test": "cd client && npm test"
  },
  "scripts": {
    "build": "cd client && npm run build",
    "start": "cd client && npm start",
    "dev": "cd client && npm run dev",
    "debug": "cd client && npm run debug"
  }
}

